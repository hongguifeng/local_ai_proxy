# @llm-proxy/server

Node.js 24 CLI and runtime for LLM Proxy. Install the matching `@llm-proxy/contracts` package, then run `llm-proxy --help`.

Relative `--config-file` and `--log-root` paths are resolved from the process working directory. Use absolute paths for service deployments.
