# 历史请求智能摘要 TODO

## 使用说明

按阶段顺序实施；同一阶段内可并行处理标记为“可并行”的项目。完成项目后勾选复选框，并补充对应测试或提交链接。

## 阶段一：配置与基础模型

- [x] 在 `src/config/config-schema.ts` 增加全局 `summary_model` schema 和类型。
- [ ] 更新默认配置、normalizer、validation、repository 及迁移逻辑（旧配置无该字段时保持禁用）。
- [ ] 定义 API 类型枚举：OpenAI Chat、OpenAI Responses、Anthropic Messages。
- [ ] 为超时、最大输入字符数、headers 和 URL 增加边界校验。

## 阶段二：总结数据持久化

- [x] 新增 `history_summaries` SQLite 表，主键为 `record_id`。
- [x] 增加 `source_hash`、`prompt_version`、状态、结果、错误和时间字段。
- [ ] 实现查询、插入、更新、删除及并发生成状态控制。
- [ ] 删除历史 record/task 时级联清理对应摘要。
- [ ] 编写数据库迁移和回滚验证。

## 阶段三：服务端总结模块

- [x] 从单条 record 提取 messages/input，保持原始顺序并生成编号。
- [ ] 去除 headers、Key、二进制和重复 JSON；保留工具调用及多模态类型摘要。
- [ ] 实现最大字符数限制，按消息边界截断并加入 `[已截断]` 标记。
- [x] 编写阶段区间摘要 prompt，要求输出 `segments`，禁止逐条复述和臆造。
- [x] 实现 OpenAI Chat、Responses、Anthropic 三种请求适配器。
- [ ] 增加超时、响应大小限制、一次 JSON 修复重试和错误分类。
- [ ] 对模型响应执行 schema 校验，拒绝不合法结果。

## 阶段四：管理端 API

- [x] `GET /api/settings/summary-model`：返回配置。
- [x] `PUT /api/settings/summary-model`：校验并保存配置。
- [x] 增加总结模型连接测试 API，复用 target-check 的错误格式。
- [x] `GET /api/logs/:id/summary`：返回单条 record 的摘要状态或缓存结果。
- [x] `POST /api/logs/:id/summary`：生成摘要；处理 202、409、失败和缓存命中。

## 阶段五：历史日志 UI

- [x] 在历史日志列表上方按钮区增加“总结模型配置”按钮和“未配置/已配置”状态。
- [x] 实现类似转发地址测试的配置弹窗：API 类型、URL、模型、Key、Headers 和超时（默认 180 秒）；配置存在即启用且不限制输入 token。
- [ ] 实现保存、取消、清空 Key、连接测试、校验错误和保存成功提示。
- [x] 在单条历史请求详情标题栏增加“智能总结”按钮。
- [ ] 实现无配置置灰、生成中状态、重复点击保护和失败提示。
- [x] 展示标题、概览、阶段区间时间线、关键点、决策、问题和证据编号。
- [x] 增加重新生成、复制 Markdown、查看模型与生成时间功能。
- [ ] 切换记录和刷新页面后恢复摘要状态；接入轮询或现有刷新机制。

## 阶段六：测试与质量门禁

- [ ] 配置 schema和旧配置兼容测试。
- [ ] 数据库迁移、摘要 CRUD、删除级联和并发测试。
- [ ] 三种 API 协议的请求构造、4xx、超时、空响应和非法 JSON 测试。
- [ ] 单条请求排序、超长截断、多模态和工具调用整理测试。
- [ ] 管理 API 的成功、缓存命中、生成中、失败和鉴权安全测试。
- [ ] UI 测试覆盖弹窗、配置保存、按钮状态、摘要展示和重新生成。
- [ ] 确认原始 JSON 查看、搜索、导出行为未改变。

## 阶段七：文档与发布

- [x] 更新 `README.md` 和 `README.cn.md` 的功能说明与配置步骤。
- [ ] 更新 API/模块设计文档和变更记录。
- [x] UI 变更后运行 `npm run regen:ui-baselines`，更新 `doc/ui_*.png` 及视觉基线哈希。
- [ ] 执行完整测试、构建和人工验收，记录模型费用与失败场景。

## 完成验收标准

- [ ] 用户可在历史日志页面配置并测试总结模型。
- [ ] 用户点击单条请求的按钮后，能看到按连续编号区间归纳的摘要。
- [ ] 原始请求 JSON 保持可查看、可搜索、可导出。
- [ ] 摘要可缓存和重新生成，失败不影响历史页面。
- [ ] 总结请求仍遵循现有管理工具的配置和日志约定。



